import 'package:flutter/material.dart';

class TransaksiAdminPage extends StatelessWidget {
  const TransaksiAdminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}